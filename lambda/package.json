{
  "name": "lambda-root",
  "private": true,
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "build": "npm-run-all --parallel build:*",
    "build:authorizer": "cd authorizer && npm install && npm run build",
    "build:sample": "cd sample && npm install && npm run build",
    "build:sample2": "cd sample2 && npm install && npm run build",
    "build:sample3": "cd sample3 && npm install && npm run build"
  }
}
